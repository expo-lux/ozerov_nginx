base_domain: some_domain
auth_file: /etc/nginx/.passwd 


htpasswd_credentials:
  - path: /etc/nginx/.passwd
    name: user
    password: 'test'
    owner: root
    group: www-data
    mode: 'u+rw,g+r'